# 项目协作约定

适用于本仓库。当前实现与开发入口见 [README.md](README.md)；[UX_RESEARCH.md](UX_RESEARCH.md) 是历史研究，不作为当前功能禁令。审计依据见 [指令审计记录](docs/instruction-audit.md)。

## 执行与授权

- 部署（含预览上传）、推送或合并、远程 D1 迁移、创建邀请码、修改云端资源或密钥，以及发送消息，需要本次任务或既有上下文明确授权。开发、审计或测试请求本身不授予这些权限。
- 不丢弃工作区已有改动，不读取或输出与任务无关的密钥；不将 `.dev.vars`、凭据或真实用户图片写入提交、日志、报告或测试夹具。

## 业务约束

- 保持移动优先的照片转贴纸、收藏、手帐拼贴和 PNG 导出流程；保留 320px 窄屏、键盘操作、对话框焦点和减少动效支持。
- 保持邀请码注册、账号间数据隔离、IndexedDB 离线副本、D1 记录同步和私有 R2 图片存储。不要将历史“无需注册／仅本地”方案恢复为现状。
- 保留 150 个账号、每账号 50 MiB 图片的限制；README 中 R2 达到 7 GB 停止发码是运维规则，不是已实现的自动限额。
- 图片外发用于用户发起的抠图和已配置的 AI 识别；保持现有鉴权、输入验证、大小限制、限流和敏感日志边界。改变供应商或扩大数据用途需明确授权。
- 保留静态资源的第三方许可。业务变更获授权时，同步更新对应文档与必要验证，避免把现有实现冻结为永久规则。

## 验证与交付

- 使用 Node.js 24（见 `.nvmrc` 和 `package.json`）。按改动选择检查：类型 `npm run typecheck`，静态检查 `npm run lint`，单测 `npm run test:unit -- <测试文件>`，构建 `npm run build`，端到端 `npm run test:e2e`。
- UI 流程变更验证相关浏览器路径和移动布局；Worker 配置或打包变更可运行 `npm run check:cloudflare`（构建和部署 dry-run）。真实付费接口调用不作为默认验证。
- 纯文档改动检查差异、链接、命令是否存在及指令冲突即可；不为此新增实现镜像测试或执行整套应用测试。检查通过后，只有新改动、失败或未解决风险才扩大验证。
- 简洁说明改了什么、依据、实际验证结果和未验证事项；不得把历史测试记录或子代理推测写成此次已验证结论。
